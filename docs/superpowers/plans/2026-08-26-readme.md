# AI 短劇編劇 README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在專案根目錄建立與真實技能功能一致的繁體中文 README，連同 MIT LICENSE 提交並推送至 GitHub `origin/main`。

**Architecture:** README 是單一入口文件，內容直接連結技能原始碼、三份 reference 與既有驗證證據，不複製完整內部文件。安裝章節採 OpenAI 官方個人技能位置 `$HOME/.agents/skills`，分別提供 Windows PowerShell 與 macOS／Linux 命令；發布階段只更新 `main`，不改動既有 `v0.1.0` Tag 或 Release。

**Tech Stack:** Markdown、PowerShell、Bash、Git、GitHub CLI、Codex standalone skills

## Global Constraints

- README 使用繁體中文。
- 必須包含：專案解決什麼問題、主要功能、安裝方法、使用方法、輸入輸出範例。
- Windows、macOS、Linux 均有可執行的安裝說明。
- 功能聲明必須能追溯至 `skill-src/ai-short-drama-screenwriter/`。
- 使用官方個人技能位置 `$HOME/.agents/skills`，目標資料夾為 `ai-short-drama-screenwriter`。
- 使用者只要求劇本時，不宣稱輸出分鏡或影片提示詞。
- 不新增套件、安裝腳本、plugin manifest 或技能行為。
- 保留既有 `v0.1.0` Tag 與 Release，不重寫或移動它們。
- `LICENSE` 使用現有 MIT 文字：`Copyright (c) 2026 POUND0423`。
- 最終推送到 `origin/main`，並驗證遠端 README 與 LICENSE 存在。

---

## File Structure

- Create: `README.md` — 專案入口、安裝、使用、範例、驗證與授權。
- Existing untracked: `LICENSE` — MIT License，與 README 在同一提交發布。
- Reference only: `skill-src/ai-short-drama-screenwriter/SKILL.md` — 功能邊界與呼叫方式。
- Reference only: `skill-src/ai-short-drama-screenwriter/references/workflow.md` — 八階段流程。
- Reference only: `skill-src/ai-short-drama-screenwriter/references/format.md` — 格式與短例。
- Reference only: `skill-src/ai-short-drama-screenwriter/references/checklists.md` — 審閱清單。
- Reference only: `validation/ai-short-drama-screenwriter/` — RED、GREEN 與觸發微測試證據。

### Task 1: 建立並驗證根目錄 README

**Files:**
- Create: `README.md`
- Verify: `LICENSE`
- Reference: `docs/superpowers/specs/2026-08-26-readme-design.md`

**Interfaces:**
- Consumes: `SKILL.md` 的任務路由與輸出邊界、三份 references 的功能與格式、validation 的測試結果。
- Produces: 一份可直接在 GitHub 顯示、可依命令安裝、可由新使用者理解的 `README.md`。

- [ ] **Step 1: 執行 README 缺失基準檢查**

Run:

```powershell
if (-not (Test-Path -LiteralPath 'README.md')) {
    Write-Error 'README.md missing'
    exit 1
}
```

Expected: FAIL，訊息包含 `README.md missing`。

- [ ] **Step 2: 建立 README.md**

Create `README.md` with exactly this content:

````markdown
# AI 短劇編劇

> 以繁體中文完成短篇／豎屏短劇的選題、結構、角色、分場、臺詞、衝突、反轉、格式與修改。

[GitHub Release v0.1.0](https://github.com/POUND0423/AI-drama-pound/releases/tag/v0.1.0) · [MIT License](LICENSE)

`ai-short-drama-screenwriter` 是一個可供 ChatGPT 桌面版 Codex、Codex CLI 與 IDE extension 使用的 standalone skill。它把故事前提與製作限制整理成可見、可聽、可拍攝，且有明確戲劇推進的短劇內容。

## 專案解決什麼問題

一般 AI 短劇草稿常見以下問題：

- 故事只有概念，缺乏可持續推進的角色目標、阻力與代價。
- 角色轉變沒有前置事件，人物動機在場次間跳躍。
- 臺詞只解說背景，缺少試探、迴避、反應與潛臺詞。
- 反轉沒有前文線索，集尾只中斷事件而沒有下一步問題。
- 場次缺少內外景、地點或時間，難以直接進入製作。
- 使用者只要求劇本，輸出卻越界加入分鏡或影片模型提示詞。

本技能用明確的任務路由、八階段編劇流程、標準短劇格式與修改清單，讓輸出保持可拍性、敘事推進與交付範圍一致。

## 主要功能

- **完整編劇流程**：創作簡報、題材定位、全劇與單集結構、角色關係、分場、臺詞、衝突／情緒／反轉／鉤子、初稿與修改。
- **單點創作**：只處理指定的選題、結構、角色、分場、臺詞、衝突、反轉或格式，不強制重做完整專案。
- **標準短劇格式**：支援場次、內景／外景、地點、時間、動作、角色名稱、臺詞及畫面文字。
- **劇本審閱與修改**：按影響程度提供問題、段落證據、造成的影響與可執行修正方向。
- **限制保持**：保留使用者指定的集數、時長、觀眾、類型、平台、預算、場景與交付形式。
- **繁體中文輸出**：預設使用繁體中文；其他語言可在提示中指定。
- **跨技能銜接**：混合需求先完成劇本，再銜接環境中已安裝且適用的分鏡或影片提示詞技能。

## 安裝方法

### 前置條件

- 已安裝 [Git](https://git-scm.com/)。
- 已安裝支援 standalone skills 的 ChatGPT 桌面版 Codex、Codex CLI 或 IDE extension。

Codex 的個人技能位置為 `$HOME/.agents/skills`。每個技能都必須是獨立資料夾，且資料夾內直接包含 `SKILL.md`。詳見 [OpenAI 官方技能文件](https://learn.chatgpt.com/docs/build-skills)。

### Windows PowerShell

```powershell
git clone --depth 1 https://github.com/POUND0423/AI-drama-pound.git

$repoPath = Join-Path (Get-Location) 'AI-drama-pound'
$skillPath = Join-Path ([Environment]::GetFolderPath('UserProfile')) '.agents\skills\ai-short-drama-screenwriter'

New-Item -ItemType Directory -Force -Path $skillPath | Out-Null
Copy-Item -Path (Join-Path $repoPath 'skill-src\ai-short-drama-screenwriter\*') -Destination $skillPath -Recurse -Force

Test-Path (Join-Path $skillPath 'SKILL.md')
```

最後一行應輸出 `True`。

### macOS／Linux

```bash
git clone --depth 1 https://github.com/POUND0423/AI-drama-pound.git

mkdir -p "$HOME/.agents/skills/ai-short-drama-screenwriter"
cp -R "AI-drama-pound/skill-src/ai-short-drama-screenwriter/." \
  "$HOME/.agents/skills/ai-short-drama-screenwriter/"

test -f "$HOME/.agents/skills/ai-short-drama-screenwriter/SKILL.md" \
  && echo "Skill installed"
```

成功時會輸出 `Skill installed`。Codex 通常會自動偵測技能；若沒有出現，請重新啟動 Codex。

## 使用方法

### 顯式呼叫

在提示中直接加入技能名稱：

```text
使用 $ai-short-drama-screenwriter，把以下故事前提規劃成 8 集、每集 90 秒的都市懸疑短劇。主要場景不超過 3 個；先提供創作簡報、角色關係、全劇節拍與第一集劇本。

故事前提：外送員每天替一位失憶老人送餐，最後發現老人可能是失蹤多年的外公。
```

### 隱式呼叫

也可以直接提出符合技能範圍的需求，例如：

```text
只修改這段短劇重逢戲的臺詞，增加潛臺詞，不要重做人物設定或全劇大綱。
```

技能允許隱式啟動；是否啟動取決於請求是否符合 `SKILL.md` 的使用範圍。

## 輸入輸出範例

### 輸入

```text
使用 $ai-short-drama-screenwriter，將「外送員替失憶老人送餐，逐漸發現老人是失蹤多年的外公」發展成 8 集都市懸疑短劇。每集 90 秒，主要場景不超過 3 個。請提供必要假設、創作簡報、角色關係、全劇節拍與第一集劇本。
```

### 輸出節錄

```text
必要假設
- 觀眾：偏好都市情感懸疑的短劇觀眾
- 製作限制：主要場景為外送站、老公寓與主角家

創作簡報
- 類型：都市溫情懸疑
- 主角目標：確認老人身分，同時查明家人隱瞞的往事
- 主要阻力：老人記憶破碎，主角母親阻止他繼續調查

角色關係
- 阿澤：外送員；從同情老人轉為追查自己的家族秘密
- 陳伯：失憶老人；保留與阿澤童年有關的零碎記憶
- 美玲：阿澤母親；知道老人身分，但害怕舊事再次傷害家人

全劇節拍（節錄）
- 第 1 集：阿澤在老人門口撿到自己童年的照片；母親要求他停止送餐。
- 第 4 集：老人短暫恢復記憶，叫出阿澤父親的名字。
- 第 8 集：阿澤確認血緣真相，必須決定公開秘密或保護母親。

第 1 集

1. 內景・老公寓走廊・夜

阿澤提著餐袋停在 302 室門前。門縫下壓著一張泛黃照片。

照片裡，五歲的阿澤坐在一名男子肩上。男子的臉被撕掉一半。

阿澤
陳伯，您的晚餐到了。

門內傳來老人顫抖的聲音。

陳伯（OS）
小澤，你終於回來了。

阿澤僵住。他從未告訴老人自己的小名。

切黑。
```

實際輸出會依故事前提、限制與指定交付形式改變，不保證與範例逐字相同。

## 功能邊界

- 只要求劇本時，不輸出逐鏡分鏡、景別、運鏡或 AI 影片模型參數。
- 本倉庫不包含分鏡或影片提示詞技能；混合交付需要環境中另有適用技能。
- 涉及當前平台偏好、演算法或市場趨勢時，必須先查證，不能把舊資訊當成現況。

## 專案結構

```text
AI-drama-pound/
├── skill-src/ai-short-drama-screenwriter/
│   ├── SKILL.md
│   ├── agents/openai.yaml
│   └── references/
│       ├── workflow.md
│       ├── format.md
│       └── checklists.md
├── validation/ai-short-drama-screenwriter/
├── docs/superpowers/
├── LICENSE
└── README.md
```

## 驗證狀態

- [RED 基線測試](validation/ai-short-drama-screenwriter/baseline-results.md)：記錄沒有技能時的格式缺口。
- [GREEN 行為測試](validation/ai-short-drama-screenwriter/skill-results.md)：四個情境最終全部通過。
- [觸發邊界微測試](validation/ai-short-drama-screenwriter/trigger-microtest-results.md)：A–F 最終各為 5/5。
- 官方技能結構驗證：通過。

## 常見問題

### 安裝後找不到技能

確認以下檔案直接存在：

```text
$HOME/.agents/skills/ai-short-drama-screenwriter/SKILL.md
```

若路徑正確但技能仍未出現，重新啟動 Codex。

### 可以直接產生分鏡或影片提示詞嗎？

此技能本身負責短劇編劇。若同時要求劇本與分鏡或影片提示詞，它會先完成劇本，再嘗試銜接環境中已安裝的適用技能。

## 授權

本專案採用 [MIT License](LICENSE)。
````

- [ ] **Step 3: 執行內容契約檢查**

Run:

```powershell
$readme = Get-Content -Raw -LiteralPath 'README.md'
$required = @(
    '## 專案解決什麼問題',
    '## 主要功能',
    '## 安裝方法',
    '### Windows PowerShell',
    '### macOS／Linux',
    '## 使用方法',
    '## 輸入輸出範例',
    '$ai-short-drama-screenwriter',
    '.agents/skills/ai-short-drama-screenwriter',
    '1. 內景・老公寓走廊・夜',
    '## 功能邊界',
    '## 驗證狀態',
    '## 授權'
)

$missing = $required | Where-Object { -not $readme.Contains($_) }
if ($missing) {
    throw "README missing required content: $($missing -join ', ')"
}

'README content contract passed'
```

Expected: PASS，輸出 `README content contract passed`。

- [ ] **Step 4: 檢查相對連結與檔案存在**

Run:

```powershell
$paths = @(
    'LICENSE',
    'skill-src/ai-short-drama-screenwriter/SKILL.md',
    'validation/ai-short-drama-screenwriter/baseline-results.md',
    'validation/ai-short-drama-screenwriter/skill-results.md',
    'validation/ai-short-drama-screenwriter/trigger-microtest-results.md'
)

$missing = $paths | Where-Object { -not (Test-Path -LiteralPath $_) }
if ($missing) {
    throw "Missing linked file: $($missing -join ', ')"
}

'README linked files passed'
```

Expected: PASS，輸出 `README linked files passed`。

- [ ] **Step 5: 在安全暫存目錄驗證 Windows 安裝後的技能結構**

Run:

```powershell
$tempBase = [System.IO.Path]::GetFullPath([System.IO.Path]::GetTempPath())
$testRoot = Join-Path $tempBase ('ai-drama-readme-' + [guid]::NewGuid().ToString('N'))
$target = Join-Path $testRoot '.agents\skills\ai-short-drama-screenwriter'

New-Item -ItemType Directory -Force -Path $target | Out-Null
Copy-Item -Path 'skill-src\ai-short-drama-screenwriter\*' -Destination $target -Recurse -Force

if (-not (Test-Path -LiteralPath (Join-Path $target 'SKILL.md'))) {
    throw 'Installed skill is missing SKILL.md'
}

$resolved = [System.IO.Path]::GetFullPath($testRoot)
if (-not $resolved.StartsWith($tempBase, [System.StringComparison]::OrdinalIgnoreCase)) {
    throw 'Refusing to remove path outside the system temp directory'
}

Remove-Item -LiteralPath $resolved -Recurse -Force
'Windows installation structure passed'
```

Expected: PASS，輸出 `Windows installation structure passed`；只刪除本次建立且已確認位於系統暫存目錄下的唯一資料夾。

- [ ] **Step 6: 檢查 Markdown、占位標記與技能結構**

Run:

```powershell
git diff --check
rg -n -i '(TO[D]O|TB[D]|FIX[M]E|CHANGE[M]E|PLACE[H]OLDER)' README.md
```

Expected: `git diff --check` exit 0；`rg` exit 1 且沒有輸出，表示沒有占位標記。

Run official skill validator:

```powershell
$python = 'C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
$validator = 'C:\Users\user\.codex\skills\.system\skill-creator\scripts\quick_validate.py'
$skill = 'C:\Users\user\Downloads\AI Drama\skill-src\ai-short-drama-screenwriter'
$yamlDependency = 'C:\Users\user\Downloads\AI Drama\.validation-deps\pyyaml-sandbox'

& $python -X utf8 -c "import runpy,sys; sys.path.insert(0, r'$yamlDependency'); sys.argv=[r'$validator', r'$skill']; runpy.run_path(r'$validator', run_name='__main__')"
```

Expected: exit 0，輸出 `Skill is valid!`。

- [ ] **Step 7: 審查差異並提交 README 與 LICENSE**

Run:

```powershell
git status --short
git diff -- README.md LICENSE
```

Expected: `README.md` 與 `LICENSE` 是本次尚未提交的交付檔；已提交的設計與計畫不應出現在未提交 diff。

Stage only the deliverables and commit:

```powershell
git add -- README.md LICENSE
git diff --cached --check
git diff --cached --name-only
git commit -m "docs: add README and MIT license"
```

Expected: staged names only include `README.md` and `LICENSE`；commit exit 0。

### Task 2: 發布到 GitHub 並驗證 main

**Files:**
- Publish: committed `README.md`
- Publish: committed `LICENSE`
- Preserve: annotated Tag `v0.1.0`

**Interfaces:**
- Consumes: Task 1 的 README／LICENSE commit，以及先前已提交的 README 規格與本實作計畫。
- Produces: GitHub `POUND0423/AI-drama-pound` 的 `main` 顯示 README 並包含 MIT LICENSE。

- [ ] **Step 1: 推送 main**

Run:

```powershell
git push origin main
```

Expected: push exit 0，`main -> main` 或 `Everything up-to-date`。

- [ ] **Step 2: 驗證本機與遠端同步且 Tag 未移動**

Run:

```powershell
git fetch origin main --tags
git status --short --branch
git rev-list --left-right --count main...origin/main
git rev-parse 'v0.1.0^{}'
```

Expected:

- `git status` 顯示 `main...origin/main`，沒有 ahead／behind。
- rev-list 輸出 `0 0`。
- `v0.1.0^{}` 仍輸出 `df239aed7b325ce1b4fc3a34413f2e93115ba1a7`。

- [ ] **Step 3: 驗證 GitHub 上的 README 與 LICENSE**

Run:

```powershell
gh api 'repos/POUND0423/AI-drama-pound/contents/README.md?ref=main' --jq '.path'
gh api 'repos/POUND0423/AI-drama-pound/contents/LICENSE?ref=main' --jq '.path'
gh repo view POUND0423/AI-drama-pound --json url,defaultBranchRef,licenseInfo
```

Expected:

- 第一行輸出 `README.md`。
- 第二行輸出 `LICENSE`。
- 倉庫網址為 `https://github.com/POUND0423/AI-drama-pound`。
- 預設分支為 `main`。
- GitHub 識別 License key 為 `mit`；若授權偵測尚未完成，README 與 LICENSE 路徑成功即證明檔案已發布，稍後再查 licenseInfo。

- [ ] **Step 4: 最終本機狀態檢查**

Run:

```powershell
git status --short --branch
git log -3 --oneline --decorate
```

Expected: 工作目錄乾淨，`main` 與 `origin/main` 同步；最近提交包含 `docs: add README and MIT license`、本實作計畫與 `docs: define README design`。
